<%= render "page_header" do %>
  <%= render "page_header_breadcrumb", title: @user.email do %>
    <li class="breadcrumb-item"><%= link_to "Admin", admin_path %></li>
    <li class="breadcrumb-item"><%= link_to "Users", admin_users_path %></li>
  <% end %>

  <%= render "page_header_actions" do %>
    <%= link_to "Edit user", edit_admin_user_path(@user), class: "btn-primary" %>
    <%= link_to "Delete user", @user, class: "btn-white", data: { method: :delete, confirm: "Are you sure?" } %>
  <% end %>
<% end %>

<div class="page-body">
  <div class="container-xl">
    <div class="card">
      <div class="card-header">
        <h3 class="card-title">User details</h3>
      </div>
      <div class="card-body">
        <dl class="grid grid-cols-1 gap-y-4 sm:grid-cols-3">
          <dt class="text-sm font-medium text-gray-500">Email</dt>
          <dd class="text-sm text-gray-900 sm:col-span-2"><%= @user.email %></dd>

          <dt class="text-sm font-medium text-gray-500">Created at</dt>
          <dd class="text-sm text-gray-900 sm:col-span-2"><%= l(@user.created_at, format: :long) %></dd>

          <dt class="text-sm font-medium text-gray-500">Updated at</dt>
          <dd class="text-sm text-gray-900 sm:col-span-2"><%= l(@user.updated_at, format: :long) %></dd>
        </dl>
      </div>
    </div>
  </div>
</div>
